# 任务要求

实现提取两个tag版本差异的更新信息提取
实现非法提交记录预警
实现接入jekins自动打包发布svn和gitlab联动
实现发布信息检索看板

## 任务输出

web前端控制页面
后台管理服务

## 基础要求

### 1、验证 GIT用户提交的commit 是否符合如下格式：

1、提交commit：用灵码生成提交日志，人工校正，提交日志内容参照以下：

变更类型：
□ 无配置项改动 □有配置项改动

更新内容：
[A为功能增加 F为BUG修复]：

1. A 移除干扰用户绑定数据采集(DA)的逻辑
2. F 信号定时开关新增清除指令并优化控制逻辑

配置项改动[详见相关软件配置文件管理]：
[System]
DeviceType=0

关联性改动[选填]：
PXX板卡硬件版本:
信号子模块硬件版本:
控制子模块硬件版本:
后台服务程序版本:
场景管理程序版本:
数学仿真软件版本：
轨迹仿真软件版本：
采集回放仪信号处理软件版本：
采集回放仪数据管理软件版本：
采集回放仪通信控制软件版本：
采集回放仪板载显控软件版本：
采集回放仪人机显控软件版本：
导航增强上位机软件版本：
信号单元后台软件版本：
基准单元后台软件版本：
信号单元数学仿真软件版本：
信号单元硬件程序版本：
基准单元硬件程序版本：
阵列天线采集卡软件版本：
阵列天线时钟加权软件版本:
阵列天线系统加权软件版本：
阵列天线信号转发软件版本：
阵列天线上位机软件版本：

### 2、TAG生成工具：

① 抓取并显示最新develop分支到上一个tag的所有提交记录
② 合并提交记录
③ 生成以下文档

# 软件发布规范示例

当前版本号： VA.4.1.154
Git提交哈希： 271b688781e11ce76090b0ff1d281ec8d1dcd991
变更类型：
□ 无配置项改动 □ 有配置项改动

更新内容:
[A为功能增加 F为BUG修复]：

1. A 移除干扰用户绑定数据采集(DA)的逻辑
2. F 信号定时开关新增清除指令并优化控制逻辑

配置项改动[详见相关软件配置文件管理]：
[System]
DeviceType=0

关联性改动[选填]：
PXX板卡硬件版本:
信号子模块硬件版本:
控制子模块硬件版本:
后台服务程序版本:
场景管理程序版本:
数学仿真软件版本：
轨迹仿真软件版本：
采集回放仪信号处理软件版本：
采集回放仪数据管理软件版本：
采集回放仪通信控制软件版本：
采集回放仪板载显控软件版本：
采集回放仪人机显控软件版本：
导航增强上位机软件版本：
信号单元后台软件版本：
基准单元后台软件版本：
信号单元数学仿真软件版本：
信号单元硬件程序版本：
基准单元硬件程序版本：
阵列天线采集卡软件版本：
阵列天线时钟加权软件版本:
阵列天线系统加权软件版本：
阵列天线信号转发软件版本：
阵列天线上位机软件版本：

是否影响其他功能 □ 否 □ 是（例：影响功率控制）
测试验证： 测试状态：□ 自测试通过 □ 研发测试复验通过
发布人： 蒋鑫

④人工校验核对
⑤发布自动推tag

### 3、账号权限管理

①每个项目配置一个GITLAB管理员账号；
②接入LDAP；
③保存操作日志；

### 4、commit记录查看

①查询分支提交记录
②按照commit规范督查

### 5、正式发布

正式发布只能通过主分支，三天发布一个版本，可以通过其他分发布测试版本，测试版本不允许放在正式发布的文件夹下面，测试版本必须打上test标签。（抽查）
